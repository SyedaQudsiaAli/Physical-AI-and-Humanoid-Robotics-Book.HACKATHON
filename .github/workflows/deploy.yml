
name: Deploy Docusaurus to GitHub Pages

on:
  push:
    branches:
      - main
      - master

jobs:
  build:
    name: Build Docusaurus
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: Physical-AI-and-Humanoid-Robotics-Book/package-lock.json

      - name: Install dependencies
        working-directory: Physical-AI-and-Humanoid-Robotics-Book
        run: npm ci

      - name: Build website
        working-directory: Physical-AI-and-Humanoid-Robotics-Book
        run: npm run build

      - name: Upload Build Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: Physical-AI-and-Humanoid-Robotics-Book/build

  deploy:
    name: Deploy to GitHub Pages
    needs: build

    permissions:
      pages: write
      id-token: write

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4